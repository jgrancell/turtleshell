package main

import (
    "fmt"
    "os"
)

func runner_identify(args []string) map[string]string {
    return := make(map[string]string)

    if args[1] == "run" || args[1] == "." {
        return["runner"] = "file"
    } else if _, e{
        return "farts"
    }
}
